server:
  address: ":8080"
  tls:
    cert: ""
    key: ""

database:
  dsn: ""
  max_open_conns: 25
  max_idle_conns: 10

jwt:
  secret: "change-me-in-production"
  access_ttl: 15m
  refresh_ttl: 720h

log:
  level: info
